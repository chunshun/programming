#include <cstdio>
#include <algorithm>
#include <string.h>
using namespace std;
int main(int argc, char const *argv[])
{
    // printf("hello world");
    // int a[10]={2,1,3,4,6,3,9};
    int arr[10];
    memset(arr,0,sizeof(arr));
    
    for(int i = 0; i < 10; i++)
    {
        printf("%d\t",arr[i]);
        /* code */
    }
    
    char *s;
    int aa=1;
    int b=3;
    swap(aa,b);
    // scanf("%s",s);
    // printf("%d,%d",aa,b);
    // printf("%s",s);
    return 0;
}
